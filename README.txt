It was added for each directory a file.(txt) named "how to compile", so that its way much easier
to know which are the key words needed for plots, mass ranges etc. 

Many of these files even contain the expected output (in case the output is expected), or what 
the program produces (in particular for histo versions).